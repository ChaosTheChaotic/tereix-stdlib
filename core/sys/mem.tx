extern {
	any malloc(size size);
	any calloc(size n, size size);
	any realloc(any p, size size);

	void free(any p);
	
	any memcpy(any dest, any src, size n);
	any memset(any s, i32 c, size n);
	any memmove(any dest, any src, size n);
	i32 memcmp(any s1, any s2, size n);
}
